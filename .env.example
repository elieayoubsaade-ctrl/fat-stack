# Backend — publishable keys, safe to expose in the browser by design.
# The database is locked down with Row Level Security; see the migration for why.
VITE_SUPABASE_URL=https://mgepcbfxijsdhmlmrtxh.supabase.co
VITE_SUPABASE_KEY=sb_publishable_...

# Analytics — PostHog *public project* key (starts phc_). Optional: without it the
# game runs normally and simply sends no analytics.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://eu.i.posthog.com
