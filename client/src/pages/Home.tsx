/** Comic Counter Carnival: compatibility route kept intentionally minimal; App renders the game. */
import GameCanvas from "@/components/GameCanvas";

export default function Home() {
  return <GameCanvas />;
}
